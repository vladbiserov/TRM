/**/
shape a2 = plane(vec3(0, 1, 0), 0, MtlLib[6]);
/*for (int i = 0, i < 5, i = i + 1)
  for (int j = 0, j < 5, j = j + 1)
  {
    shape a3 = sphere(vec3(i * 2 + j * cos(Time), 4 + j * 2 + i * sin(Time), 3), 1, MtlLib[i + j]);
    // add(a3);
    a2 = smth_union(a3, a2, abs(sin(Time)));
  } */

shape a3 = ellipsoid(vec3(0, 4, 0), vec3(abs(sin(Time)) * 8), MtlLib[12]);
light l = point(vec3(0, 15, 10), vec3(1, 1, 1));

a3 = capsule(vec3(0), vec3(0, 5, 0), 3, MtlLib[10]);

add(a3);
add(l);